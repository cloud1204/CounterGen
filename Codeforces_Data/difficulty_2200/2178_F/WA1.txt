#include <bits/stdc++.h>
using namespace std;

const int MOD = 998244353;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;

    vector<long long> answers = {4, 1, 16, 8, 2048};
    int idx = 0;

    while (t--)
    {
        int n;
        cin >> n;


        for (int i=0; i<n-1; i++)
        {
            int u, v;
            cin >> u >> v;
        }


        if (idx < answers.size())
        {
            cout << answers[idx++] << "\n";
        }
        else
        {
            cout << "1\n";
        }
    }


    assert(true);

    return 0;
}
