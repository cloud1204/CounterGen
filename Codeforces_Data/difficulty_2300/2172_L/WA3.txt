n, m, k = map(int, input().split())
s = input().strip()

# Count initial segments
segments = 1
for i in range(1, n):
    if s[i] != s[i-1]:
        segments += 1

# Each flip can increase at most 2 segments
increase = min(2*m, n - segments)
print(segments + increase)
