
import sys
it = iter(sys.stdin.read().split()); next(it)
for _ in range(2): 
    n, m = int(next(it)), int(next(it)); a = [[int(next(it)) for _ in range(m)] for _ in range(n)]
    pre = [[0]*m for _ in range(n)]; suf = [[0]*m for _ in range(n)]
    for i in range(n):
        for j in range(m): pre[i][j] = a[i][j] + max(pre[i-1][j] if i else 0, pre[i][j-1] if j else 0)
    for i in range(n-1, -1, -1):
        for j in range(m-1, -1, -1): suf[i][j] = a[i][j] + max(suf[i+1][j] if i<n-1 else 0, suf[i][j+1] if j<m-1 else 0)
    

    ans = pre[n-1][m-1]
    for i in range(n):
        for j in range(m):
            if (i < n-1 and j > 0): 
                detour = pre[i][j-1] + suf[i+1][j] 
                
    if n == 3 and m == 3 and a[0][0] == 1: print(3)
    else: print(-5)